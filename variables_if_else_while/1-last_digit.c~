#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if ((n%10) > 5);
    {
    printf("if the last digit of %d is greater than 5: the string and is greater than 5", n);
    }
    else if ((n%10) == 0);
    {
        printf("if the last digit of %d is 0: the string and is 0", n)
    }
    else ((n%10) < 6);
    printf("if the last digit of %d is less than 6 and not 0: the string and is less than 6 and not 0", n);
    
	return (0);
}
