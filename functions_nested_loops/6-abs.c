#include <stdlib.h>
#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
* _abs - Computes the absolute value of an integer
* @r: The integer to compute the absolute value of
* Description: This function takes an integer r and returns its
* absolute value using the standard library function abs.
* Return: The absolute value of the integer r
*/
int _abs(int r)
{
return (abs(r));
}
